
@model List<Materia>

@{
}

<h1>Materias</h1>
<a asp-action="AgregarOEditar" class="btn btn-outline-success"><i class="fas fa-lg"></i> Agregar</a>
<table class="table table-hover">
    <thead>
    <tr>
        <th>
            @Html.DisplayName( "Còdigo Materia")
        </th>
        <th>
            @Html.DisplayName( "Nombre de la Materia")
        </th>
        <th>
            @Html.DisplayName( "Total de horas")
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.CodigoMateria
            </td>
            <td>
                @item.NombreMateria
            </td>
            <td>
                @item.HorasTotales
            </td>
            <td>
                <a asp-action="AgregarOEditar" asp-route-id="@item.Id"><i class="fas fa-marker fa-lg"></i>Editar</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="text-danger ml-1" onclick="return confirm('Are you sure to delete this record?')"><i class="fas fa-trash-alt fa-lg"></i>Borrar</a>
            </td>
        </tr>
    }
    </tbody>
</table>
